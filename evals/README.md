# Offline evaluations

The deterministic scenarios are exercised by `tests/test_offline_agent.py`.
They cover outage, billing, CSV import, security, feature questions,
unknown customers, prompt injection, and prohibited consequential actions.
